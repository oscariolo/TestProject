name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  english_words: ^4.0.0
  provider: ^6.0.0
  firebase_auth: ^4.19.6
  google_sign_in: ^6.2.1
  firebase_core: ^2.31.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  assets:
    - lib/assets/icons/
  uses-material-design: true
  fonts:
    - family: Virgil
      fonts:
        - asset: lib/assets/fonts/Virgil.ttf
  
